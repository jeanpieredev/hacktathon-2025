import { builder } from './builder';

// Query - Las queries se definen en ./queries/user.ts
builder.queryType({});

// Mutation - Las mutaciones se definen en ./mutations/user.ts
builder.mutationType({});
